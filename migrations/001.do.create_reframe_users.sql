CREATE TABLE reframe_users (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    username VARCHAR (255) NOT NULL,
    user_password VARCHAR (255) NOT NULL,
    email VARCHAR (100) NULL
);